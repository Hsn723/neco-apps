apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  template:
    spec:
      containers:
        - name: grafana
          livenessProbe:
            periodSeconds: 10
            failureThreshold: 90
