apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: grafana-sandbox
  name: grafana
spec:
  template:
    spec:
      containers:
        - name: grafana
          livenessProbe:
            periodSeconds: 10
            failureThreshold: 90
